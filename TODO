features
 * add a level editor
    - change level file format (arbitrary size, blocks to be stored as <x> <y> <type>)
 * add an in-game menu (or, at least, a pause feature)
 * viewable highscore list
    - add user-visible replay
 * add particle class for explosions
    - render text screens into particle matrixes

misc
 * add a level pack class

bugs
 * fades cause a build-up of update events on GP2X
 * hitting a block's corner, in some cases, causes the game to freeze
